kind = "web"
previewPath = "/"
title = "Aman's Watch Vault"
version = "1.0.0"
id = "artifacts/watch-vault"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23861

[services.development]
run = "pnpm --filter @workspace/watch-vault run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/watch-vault", "run", "build" ]
publicDir = "artifacts/watch-vault/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23861"
BASE_PATH = "/"
